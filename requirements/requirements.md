# Login Requirement

A user should be able to log in using a valid username and password.

## Business Rules
- Valid username and password should allow login.
- Invalid credentials should display an error message.
- Username and password are mandatory.
- Password length must be between 8 and 20 characters.
